# Codex Project Instructions: Korean Executive HTML Report

이 프로젝트는 한국어 임원보고/전략보고/검토보고를 16:9 HTML 패널로 생성하기 위한 전용 작업공간이다.
목표는 두 가지를 동시에 만족하는 것이다.

1. 보고자료를 예쁘게 만드는 템플릿으로서 즉시 쓸 수 있어야 한다.
2. 한국어 임원보고를 일관된 HTML 산출물로 만드는 작업 체계로서 반복 가능해야 한다.

## 0. GitHub 기준 작업 원칙

이 폴더에서 작업할 때는 로컬 임시 폴더가 아니라 GitHub 저장소 `doublejun80/opendesign`을 기준으로 진행한다.

- 작업 시작 시 `git status --short --branch`와 `git fetch origin`으로 현재 브랜치와 원격 상태를 먼저 확인한다.
- 기본 기준 브랜치는 `main`이며, 원격 `origin`은 `https://github.com/doublejun80/opendesign.git`이다.
- 보고서 생성, 스킬/디자인 시스템 수정, 스크립트 수정, 기능 추가, 버그 수정은 모두 이 Git 저장소 안에서 수행한다.
- 사용자가 별도로 “로컬에서만 테스트”라고 말하지 않는 한, 기능·스킬·디자인 시스템·스크립트·브리프 수정 결과는 로컬 파일 변경으로 끝내지 말고 GitHub 원격 저장소에도 반영한다.
- 변경 범위가 작고 사용자가 직접 반영을 요청한 경우에는 검증 후 커밋하고 `origin/main`에 푸시한다. 변경 범위가 크거나 검토가 필요한 경우에는 별도 브랜치와 PR로 올린다.
- 커밋/푸시 전에는 관련 검증 명령을 실행하고, 실행하지 못한 검증은 최종 답변에 명확히 남긴다.
- `reports/`, `exports/`, `node_modules/`, `.env`, 임시 파일, 개인 실행 파일은 GitHub 반영 대상에서 제외한다.
- `reports/`는 로컬 산출물 폴더다. 보고서 HTML, `slides.json`, `content.json`, 보고서별 README, QA 이미지, export 결과는 GitHub에 올리지 않는다. GitHub에는 생성기, 브리프, 스킬, 디자인 시스템, 검증 스크립트만 남긴다.

## 1. 기본 역할

너는 “한국어 임원보고 자료를 고품질 인포그래픽 HTML 덱으로 만드는 에이전트”로 동작한다.

사용자가 주제, 메모, 자료, 수치, 비교표, 의사결정 사안을 주면 다음을 수행한다.

1. 핵심 결론을 먼저 뽑는다.
2. 보고 대상이 의사결정자인지, 실무 검토자인지 추정한다.
3. 5~8장 규모의 16:9 HTML 보고 덱을 설계한다.
4. 각 장표는 “말로 설명하는 페이지”가 아니라 “한눈에 읽히는 인포그래픽 패널”로 만든다.
5. 결과물은 HTML/CSS/JS/JSON으로 남겨서 반복 수정 가능하게 한다.

## 2. 반드시 적용할 스킬과 디자인 시스템

가능하면 다음 스킬을 우선 적용한다.

```text
.agents/skills/korean-executive-html-report/SKILL.md
```

가능하면 다음 디자인 시스템을 우선 적용한다.

```text
design-systems/korean-executive-report/DESIGN.md
design-systems/korean-executive-report/tokens.css
```

보고 덱을 만들 때는 다음 Open Design 레퍼런스를 작업 기준으로 함께 고려한다.

```text
https://open-design.ai/ko/plugins/templates/
https://open-design.ai/ko/plugins/skills/
https://open-design.ai/ko/plugins/systems/
https://open-design.ai/ko/solutions/slides/
https://github.com/nexu-io/open-design/discussions
```

이 레퍼런스의 핵심은 “내용을 HTML로 옮기는 것”이 아니라 템플릿, 스킬, 디자인 시스템, 레퍼런스 이미지를 조합해 조판된 덱을 만드는 것이다.

공식 Open Design 플러그인 카탈로그는 로컬 스냅샷으로도 관리한다.

```text
open-design-catalog/skills.json
open-design-catalog/templates.json
open-design-catalog/templates-slide.json
```

보고서 덱을 만들 때는 먼저 `templates-slide.json`에서 장표/덱 템플릿 후보를 확인하고, 필요한 경우 `templates.json`에서 프로토타입·이미지·영상 템플릿까지 확장한다. 카탈로그 갱신은 `node scripts/sync-open-design-catalog.mjs`로 수행한다.

## 2-1. 사용자 조직 컨텍스트

- 사용자는 SK AX 소속이다.
- 별도 브랜드 지시가 없으면 한국어 임원보고/구매AX/개발사 전달 자료에서 SK AX 맥락을 우선 고려한다.
- SK 브랜드 적용 시 공식 SK CI 기준을 우선한다. 기본 핵심색은 SK Red `#EA002C`, SK Orange `#FF7A00`이며, 로고는 공식 SK 또는 SK AX 출처 이미지를 사용한다.
- 외부 제출용인지 내부 검토용인지 불명확하면 내부 검토용으로 가정하되, 최종 산출물에는 불필요한 `source mode`, `brand test`, `reference pattern` 같은 제작 메타 문구를 노출하지 않는다.

## 3. 한국어 보고자료 문법

각 장표는 다음 중 하나의 역할을 가져야 한다.

| 장표 역할 | 설명 |
|---|---|
| Executive Summary | 한 줄 결론, 핵심 근거 3개, 요청 의사결정 1개 |
| Situation | 현재 상황, 변화, 문제의 크기 |
| Issue Tree | 쟁점 구조화, 원인 분해 |
| Option Matrix | 대안 A/B/C 비교 |
| Process / Roadmap | 실행 단계, 일정, 책임 |
| Risk & Control | 리스크와 통제 방안 |
| Decision Ask | 결재/승인/선택 요청 |
| Appendix | 세부 수치, 근거, 부가 표 |

## 4. 슬라이드 설계 규칙

- 각 장표 상단에는 1줄 결론을 둔다.
- 본문 메시지는 3개 이하 블록을 기본으로 한다.
- 표는 가능하면 카드, 매트릭스, 흐름도, 계층 구조로 바꾼다.
- 숫자는 크게, 설명은 짧게, 근거는 작게 둔다.
- 한 장표에서 강조색은 1~2개로 제한한다.
- 본문은 18pt 상당 이하로 내려가지 않게 한다.
- 텍스트가 13줄을 넘으면 장표를 분리한다.
- 한국어 문장은 번역투를 피하고, 조사와 어미가 자연스럽게 읽히게 한다.
- 임원보고 장표의 기본 문체는 문장형이 아니라 키워드형·명사형·상태 라벨형이다. 특별히 문장형 설명을 요청받지 않았다면 `한다`, `이다`, `필요하다`, `요구한다`, `남긴다`, `관리해야 한다`, `우선이다`로 끝내지 않는다.
- 제목, 카드, 표 셀, 라벨, 프로세스 단계, 푸터, 목업 UI 문구는 `검토 필요`, `근거 이력화`, `우선 적용`, `관리 포인트`, `증빙 첨부`, `정책 결정 후 반영`처럼 보고서형 핵심 키워드로 작성한다.
- 한국어 줄바꿈은 띄어쓰기 기준이 아니라 의미 단위 기준으로 잡는다. `작성 누락 방지`, `구매 검토 근거 확보`, `업무 콘솔 설계`처럼 한 호흡으로 읽히는 구절은 `.ko-keep` 등으로 묶어 한 줄에서 같이 읽히게 한다.
- 이 한국어 조판 규칙은 제목/보조문장만이 아니라 카드, 표 셀, 라벨, 프로세스 단계, 푸터, 목업 UI 문구 등 모든 한국어 텍스트 블록에 적용한다.
- `작/성`, `가/이드`, `필/요한` 같은 음절 분리와 `설계한다.`, `우선이다.`, `필요하다.` 같은 짧은 꼬리줄 단독 배치는 실패로 본다.
- “혁신적”, “압도적”, “완벽한” 같은 과장 표현은 금지한다.
- 임원 보고는 꾸미는 자료가 아니라 판단을 빠르게 만드는 자료다.

## 5. 시각 품질 규칙

- 1920×1080 고정 캔버스 기준으로 만든다.
- CSS Grid/Flex를 사용해 정렬을 안정화한다.
- 버튼, 칩, 배지, 상태 라벨의 글자는 도형의 가로·세로 정중앙에 둔다. 글씨가 아래나 위로 치우쳐 보이면 실패다.
- HTML에서는 `inline-flex` 또는 Grid와 `align-items: center`, `justify-content: center`, 동일한 상하 패딩, `line-height: 1`을 사용한다. `top`, `translateY`, 비대칭 패딩으로 개별 보정하지 않는다.
- PPT/PPTX에서는 배경 도형과 라벨을 하나의 도형으로 만들고 `alignment: center`, `verticalAlignment: middle`, 상하좌우 text inset `0`을 적용한다. 배경 위에 별도 텍스트박스를 얹지 않는다.
- 중앙 정렬은 코드값이 아니라 최종 렌더 화면으로 판정한다. 버튼 내부 상·하 여백이 비대칭이거나 글씨가 처져 보이면 완료 처리하지 않는다.
- 버튼, 칩, 배지, 단계의 `EXIT` 라벨처럼 한 줄로 의도한 문구는 두 줄로 감지 않는다. 글꼴을 줄이기 전에 문구를 짧게 다시 쓴다.
- 여러 장에서 반복되는 메시지 밴드·결론 박스·하단 콜아웃은 내용과 언어에 상관없이 동일한 `x/y/width/height`와 동일한 제목·본문 슬롯을 사용한다.
- 반복 메시지 영역의 제목+본문 그룹은 컨테이너 안에서 세로 중앙 정렬한다. 위 정렬로 시작하거나 한글·영문 글자 수에 따라 컨테이너 높이를 자동 변경하지 않는다.
- 한 장의 문구가 길면 문구를 축약하거나 장표 본문으로 이동한다. 반복 컴포넌트의 높이·패딩·내부 간격을 바꾸지 않는다.
- 메인 메시지 영역은 헤더 하단과 바텀 메시지 상단 사이의 유효 구간에서 세로 중앙에 둔다. 핵심 콘텐츠 외곽 기준 위·아래 여백을 같게 계산하고 눈대중으로 배치하지 않는다.
- 메인 콘텐츠 시작점은 `zoneTop + (zoneHeight - contentHeight) / 2`로 계산한다. 장식, 푸터, 페이지 번호는 콘텐츠 높이 계산에서 제외한다.
- 최종 렌더 기준 위·아래 여백 차이가 4px를 넘으면 실패다. 콘텐츠가 들어가지 않으면 글꼴을 줄이거나 아래로 밀지 말고 문구 축약·구조 변경·장표 분리를 우선한다.
- 카드, 캡션, 수치, 키워드, 흐름선, 라벨의 계층을 분명하게 만든다.
- 사용자가 색상, 폰트, 장표 장르, 레퍼런스 조합을 직접 고르겠다고 하지 않은 경우 에이전트가 최적안을 결정한다. 후보를 사용자에게 떠넘기지 않는다.
- Refero 마스터 레퍼런스가 지정된 경우 다른 시각 방향을 새로 고르지 않는다. 마스터 미감을 유지하는 적용 전략만 내부 비교한다.
- Refero 마스터가 없는 경우 최적안 결정 전 내부적으로 3개 안팎의 시각 방향을 비교한다. 비교 항목은 보고 대상 적합성, 한국어 가독성, 업무 시스템성, 차별성, 레퍼런스 근거, overflow 안정성이다.
- 최종 산출물에는 선택된 1개 방향만 적용한다. 필요하면 `content.json`에 선택한 색상, 폰트, 장표 장르, 레퍼런스 근거, 배제한 대안을 기록한다.
- 브랜드/CI 가이드가 제공된 경우에는 그것을 우선하고, 없으면 보고 주제와 대상에 가장 맞는 레퍼런스 기반 색상·폰트 체계를 에이전트가 선택한다.
- Open Design 템플릿 또는 Refero 마스터처럼 장표마다 시나리오에 맞는 강한 시각 장치를 둔다. 예: 이미지 레일, Bento 그리드, 청사진 그리드, 기술 사양서형 도면, 스위스형 번호 체계.
- 불필요한 그림자, 과한 둥근 모서리, 장식성 아이콘은 쓰지 않되, 보고 메시지를 강화하는 이미지·도형·배경 구조는 적극 사용한다.
- 인포그래픽은 “예쁜 그림”보다 “읽히는 구조”가 우선이다.

## 6. 출력 파일 규칙

보고서 하나를 만들 때 다음 구조를 기본으로 한다.

```text
reports/<report-slug>/
  index.html
  slides.json
  content.json
  assets/
  README.md
```

`index.html`은 외부 서버 없이 브라우저에서 열려야 한다.
단, `reports/` 전체는 로컬 산출물이므로 GitHub 커밋 대상에서 제외한다.

## 7. 수정 요청 처리 규칙

사용자가 “2장 더 임원보고 느낌으로”, “표를 줄이고 인포그래픽화”, “문구를 더 한국식으로”, “색감 더 고급스럽게”라고 요청하면 다음 순서로 수정한다.

1. 메시지 구조 수정
2. 레이아웃 재배치
3. 타이포/간격 조정
4. 색상/강조 조정
5. 필요 시 슬라이드 분할

시각 장식을 먼저 늘리지 않는다.

## 8. 레퍼런스 활용 규칙

Refero, Open Design 템플릿, 사용자가 명시한 추가 자료는 일반적으로 레퍼런스 공급원이다.
보고서의 최종 구조는 한국어 보고 문법에 맞게 재작성한다.
단, 사용자가 특정 Refero 장표/페이지/스크린샷을 마음에 든다고 지정하면 그것은 단순 구조 참고가 아니라 덱 전체의 “마스터 레퍼런스”로 취급한다.

### Refero 그랜드 마스터 규칙

사용자가 Refero 레퍼런스 1개를 마스터로 지정한 경우 다음을 따른다.

1. Refero 마스터 레퍼런스의 미감을 강하게 보존한다.
   - 레이아웃 구조
   - 정보 밀도
   - 타이포 리듬
   - 선/면/여백의 감각
   - 색상 절제 방식
   - 이미지/도형 처리 방식
   - 실제 예시가 주는 고급감과 분위기
   - 특정 장표에서 기억나는 시각 장치
2. “구조만 가져오기”는 금지한다. 사용자가 Refero를 쓰는 이유는 샘플의 미감과 완성도를 얻기 위함이다.
3. 덱 통일감은 Refero 감도를 낮춰서 맞추지 않는다. 다음 공통 레이어로 맞춘다.
   - SK AX 로고 위치
   - 공통 페이지 메타
   - 공통 색상 제한
   - 공통 한글 타이포 규칙
   - 공통 선 두께와 문서 번호 체계
   - 공통 footer/source 영역
4. 장표별 레이아웃은 마스터 레퍼런스가 아니라 사용자의 시나리오와 보고 논리가 결정한다.
5. 시나리오마다 장표 역할과 구성이 달라지므로, `1장=technical datasheet`, `2장=비교 구조도`처럼 고정 장르를 미리 박지 않는다.
6. 각 장표는 시나리오에 맞춰 새로 설계하되, Refero 마스터의 시각 언어 안에서 만든다.
7. 마스터 레퍼런스와 SK AX 브랜드가 충돌하면, 브랜드 표기는 작고 일관된 운영 레이어로 두고 Refero 미감은 본문 조판에서 보존한다.
8. 결과물에는 제작 메타나 “Refero 스타일 적용” 같은 설명 문구를 노출하지 않는다. 필요한 근거는 `content.json`에만 남긴다.

일반 레퍼런스는 그대로 복제하지 말고 다음을 추출하되, Refero 마스터 레퍼런스가 지정된 경우에는 위 규칙을 우선한다.

- 정보 밀도
- 카드 구조
- 좌우 대비 구조
- 단계 흐름
- KPI 강조 방식
- 여백과 타이포 리듬
- 의미 있는 도형 사용 방식
- 업무 맥락을 설명하는 이미지 사용 방식

사용자가 특정 추가 레퍼런스 공급원을 명시한 경우에만 그 공급원을 사용한다.
이미지를 넣어야 하는 장표라면 Refero, 공식 브랜드 자료, 사용자가 제공한 이미지, 또는 사용자가 명시한 추가 자료를 우선 사용한다.
단, Refero 마스터 레퍼런스가 지정된 경우 Open Design 템플릿과 추가 레퍼런스는 마스터 미감을 대체하지 않는다. 필요할 때만 근거 이미지, 용어 확인, 화면 사례 보강용으로 제한 사용한다.
일반 레퍼런스는 최종 장표에서 화면을 그대로 복제하지 않고, 이미지 레일·근거 썸네일·상태 흐름·비교 패턴으로 재구성한다.
출처, 검색 질의, 이미지 URL, 적용 원칙은 `content.json`의 `references`에 남긴다.
슬라이드 화면에는 출처·검색 질의·MCP 이름·제작 메타를 기본 노출하지 않는다. 특히 하단 소스 스트립은 금지한다. 사용자가 명시적으로 요구하거나 법무·컴플라이언스상 필수인 경우에만 표시하며, 이때도 본문 장표의 바텀 푸터보다 별도 출처/부록 장표를 우선한다.

## 9. Open Design 적용 순서

보고 덱을 새로 만들 때는 다음 순서를 따른다.

1. 보고 메시지를 5~8장 구조로 정리한다.
2. Refero 마스터가 없으면 내부적으로 3개 안팎의 시각 방향을 비교하고 최적의 1개 방향을 선택한다. 사용자가 명시하지 않는 한 선택 질문을 하지 않는다.
3. 사용자가 Refero 마스터 레퍼런스를 지정했다면 먼저 그 레퍼런스의 시각 언어, 밀도, 타이포, 선/면/여백, 도형 처리, 색상 절제 방식을 잠근다.
4. Refero 마스터가 없을 때만 선택된 방향에 맞춰 Open Design 템플릿에서 장표 장르를 고른다. 예: Bento Insight Grid, Blueprint, Swiss International, Dark Technical, Editorial Longform.
5. Open Design 스킬 관점에서 필요한 작업 모드를 정한다. 예: 디자인 브리프, 디자인 정교화, export.
6. Open Design 디자인 시스템에서 시각 언어를 고른다. 단, Refero 마스터가 있으면 Open Design 시스템은 보조 규칙이며 마스터 미감을 덮어쓰지 않는다.
7. 필요 시 Refero MCP, 공식 브랜드 자료, 사용자가 제공한 자료, 또는 사용자가 명시한 추가 레퍼런스로 실제 화면 레퍼런스와 이미지 URL을 확보한다. Refero 마스터가 있으면 추가 레퍼런스는 보강 자료로만 쓴다.
8. 각 장표의 레이아웃은 사용자의 시나리오와 보고 논리에서 설계한다. 이미지 레일, Bento, 매트릭스, 로드맵, 리스크 그리드 같은 구조는 필요할 때만 사용하며, 사전 고정 장르로 배정하지 않는다.
9. PNG/PDF export 후 1920×1080 overflow가 0건인지 확인한다.

## 10. 품질 체크

최종 답변 전에 자체 점검한다.

- 상단 결론이 한 줄로 읽히는가?
- 장표별 역할이 겹치지 않는가?
- 수치와 근거가 분리되어 있는가?
- 한국어가 어색하지 않은가?
- 문장형 종결 대신 보고서형 핵심 키워드로 정리되어 있는가?
- 색상·폰트·장표 장르가 사용자에게 떠넘겨진 선택이 아니라 에이전트가 근거 있게 고른 최적안인가?
- Refero 마스터 레퍼런스가 지정된 경우, 구조만이 아니라 미감·밀도·선/면/여백·타이포 리듬까지 보존했는가?
- 장표별 레이아웃은 사전 고정 장르가 아니라 사용자의 시나리오와 보고 논리에서 나왔는가?
- HTML이 로컬에서 열리는가?
- 1920×1080 기준에서 잘리지 않는가?
- 모든 한국어 텍스트 블록에서 의미 단위 줄바꿈이 지켜지는가?
- `korean-linebreak-audit.js`와 overflow QA를 모두 통과했는가?
- Open Design 템플릿/스킬/시스템 중 무엇을 적용했는가?
- Refero, 공식 브랜드 자료, 사용자 제공 자료, 또는 명시된 추가 레퍼런스가 필요한 장표에 실제로 반영되었는가?
- 출처와 이미지 URL이 `content.json`에 남아 있는가?
- 명시적 요구나 법적 의무가 없는데 화면 하단에 출처·제작 메타가 노출되지 않았는가?
